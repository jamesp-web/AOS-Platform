# ALIP backend — FastAPI production scaffold
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4
pydantic-settings==2.2.1
python-multipart==0.0.9      # file uploads
httpx==0.27.0                # async HTTP for OpenAI / Tavily
openpyxl==3.1.2              # Excel parse + export
python-dotenv==1.0.1
SQLAlchemy==2.0.29           # persistence (Postgres/Supabase or SQLite)
psycopg2-binary==2.9.9       # Postgres driver (Supabase) — not needed for SQLite/dev
supabase==2.4.0              # optional — supabase-py client (auth/storage)
