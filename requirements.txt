# Vercel serverless deps for api/index.py (the ALIP FastAPI backend).
# Lean vs backend/requirements.txt: no uvicorn (Vercel provides the ASGI server)
# and no supabase client (we talk to Postgres via SQLAlchemy + psycopg2 directly).
fastapi==0.110.0
pydantic==2.6.4
pydantic-settings==2.2.1
python-multipart==0.0.9
httpx==0.27.0
openpyxl==3.1.2
python-dotenv==1.0.1
SQLAlchemy==2.0.29
psycopg2-binary==2.9.9
